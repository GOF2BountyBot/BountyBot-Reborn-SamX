services:
  db:
    image: postgres:latest
    container_name: bounty_db
    restart: unless-stopped
    networks:
      - botnetv2
    env_file:
      - path: ./.env
        required: false 
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - POSTGRES_USER=bounty
      - POSTGRES_PASSWORD=bounty
      - POSTGRES_DB=bountydb
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./mappings/postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d bountydb -U bounty"]
      interval: 1s
      timeout: 5s
      retries: 10
    ports:
      - "5432:5432"

  bot-core:
    build: ./services/bot-core
    container_name: bot-core
    networks:
      - botnetv2
    depends_on:
      - db
    env_file:
      - path: ./.env
        required: false 
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./mappings/bot-core:/app/data
    restart: unless-stopped

  discord-gateway:
    build: ./services/discord-gateway
    container_name: discord-gateway
    networks:
      - botnetv2
    depends_on:
      - bot-core
    env_file:
      - path: ./.env
        required: false 
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./mappings/discord-gateway:/app/data
    restart: unless-stopped

  blender-service:
    build: ./services/blender-service
    container_name: blender-service
    networks:
      - botnetv2
    env_file:
      - path: ./.env
        required: false 
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./mappings/blender-renderer:/app/data
    restart: unless-stopped
    ports:
      - "8001:8000"

networks:
  botnetv2:
    name: botnetv2
    driver: bridge
    driver_opts:
      com.docker.network.bridge.name: botnetv2_br